#include <stdio.h>
#include <stdlib.h>
int main()
{
    // int notas1[20];

    double numeros[1000][1000];
    printf("Exibindo o tamanho\n");
    printf("O tamanho do array é: %d\n", sizeof(numeros));
    /*
    printf("Tamanho dos tipos\n==================\n");
    printf("Tamanho do int: %d bytes\n", sizeof(int));
    printf("Tamanho do char: %d bytes\n", sizeof(char));
    printf("Tamanho do float: %d bytes\n", sizeof(float));
    printf("Tamanho do double: %d bytes\n", sizeof(double));
    printf("Tamanho do array notas\n==================\n");
    printf("Tamanho do array notas1: %d bytes\n", sizeof(notas1));

    */
    return 1;
}